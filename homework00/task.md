# Настройка рабочего окружения

В этой работе мы познакомимся с основными инструментами, необходимыми для выполнения лабораторных работ. 

**Установка интерпретатора Python**

В первую очередь нам понадобится [интерпретатор](https://en.wikipedia.org/wiki/Interpreter_(computing)) языка Python. Если в вашей системе он еще не установлен, то проще всего установить Python с официального сайта.


⚠️

>**Обязательно во время установки выберите пункт добавления пути к интерпретатору в переменную окружения `PATH`**(если по какой-либо причине вы не сделали этого, то обратитесь к [официальному руководству](https://docs.python.org/3/using/windows.html#configuring-python)).


На скриншоте ниже указана версия 3.8, но в 2026 году актуальны 3.12 и выше. В репозитории на гитхабе будет 3.12, поэтому рекомендуется установить ее.

<img width="659" height="95" alt="image" src="https://github.com/user-attachments/assets/f38795d6-e948-415a-bc78-2615cc333af0" />


После установки проверьте версию интерпретатора (она должна быть 3.12 и выше), для этого откройте терминал (командную строку, CMD) и выполните следующую команду:

```python
$python --version
Python 3.8.2
```

>✅ Если вы работаете с ОС Windows, то для выполнения всех дальнейших шагов вам потребуется установить набор инструментов [Git For Windows](https://git-for-windows.github.io/) (или [WSL](https://docs.microsoft.com/ru-ru/windows/wsl/), но Git For Windows часто удобнее). Если вы никогда не работали в командной оболочке, то почитайте [UNIX Tutorial for Beginners](http://www.ee.surrey.ac.uk/Teaching/Unix/) для знакомства с основными командами.

>**Если вы работаете на Windows, то дальнейшие действия с командной строкой рекомендуется выполнять через GitBash.**

## Работа с виртуальными окружениями

Для выполнения работ мы будем использовать стандартный модуль [`venv`](https://docs.python.org/3/library/venv.html), появившийся с версии 3.3 и предназначенный для создания и управления изолированными (виртуальными) окружениями. `venv` позволяет заключить в отдельный каталог необходимые версии python-пакетов и использовать только их. Используя `venv`, вы можете устанавливать свежие версии пакетов из [Python Package Index](https://pypi.python.org/pypi), при этом не получая проблем с совместимостью версий загруженных пакетов и тех, что уже имеются в системе.

Создадим отдельный каталог `~/.virtualenvs` (некоторые редакторы, например, [VSCode](https://code.visualstudio.com/), по умолчанию полагаются именно на такое название и расположение каталога при поиске доступных интерпретаторов) для виртуальных окружений и новое окружение с именем `cs102`:

```python
$ mkdir ~/.virtualenvs
$ python -m venv ~/.virtualenvs/cs102
$ which python
~/.pyenv/shims/python
$ source ~/.virtualenvs/cs102/bin/activate # Scripts вместо bin, если вы на Windows
(cs102) $ which python
~/.virtualenvs/cs102/bin/python
(cs102) $ deactivate
$ which python
~/.pyenv/shims/python
```

> **Знак `$` означает приглашение к вводу команд и копировать его и все, что до него, в терминал не нужно.**

Для быстрого переключения на нужное виртуальное окружение рекомендуется добавить следующую функцию в файл `~/.bashrc` (если у вас MacOS, то `~/.zshrc`), который хранится в корневом каталоге вашего пользователя. Обычно этот файл является скрытым, поэтому может потребоваться сделать все скрытые файлы видимыми, чтобы этот файл найти.

Если такого файла в вашей системе еще нет, то его можно создать так:

```python
$ echo "" >> ~/.bashrc  # или ~/.zshrc для MacOS
```

Теперь файл можно увидеть и отредактировать. Добавьте в него функцию `workon`, которая будет отвечать за запуск виртуального окружения. Обратите внимание, действия выполняются в этой функции и сравните их с теми, что вы выполнили выше для создания виртуального окружения. Сохраните изменения в файле `~/.bashrc` и выполните команду `. ~/.bashrc` (или `source ~/.bashrc`), чтобы функция стала доступна в текущем сеансе.

Для Ubuntu и MacOS:

```python
function workon() {
    if test -z "$1" ; then
        echo "Specify the name of the virtual environment"
    elif test ! -f "$HOME/.virtualenvs/$1/bin/activate" ; then
        echo "Environment doesn't exist"
    else
        deactivate 2> /dev/null
        source "$HOME/.virtualenvs/$1/bin/activate"
    fi
}
```

Для Windows:

```python
function workon() {
    if test -z "$1" ; then
        echo "Specify the name of the virtual environment"
    elif test ! -f "$HOME/.virtualenvs/$1/Scripts/activate" ; then
        echo "Environment doesn't exist"
    else
        deactivate 2> /dev/null
        source "$HOME/.virtualenvs/$1/Scripts/activate"
    fi
}
```

> Файл `.bashrc` можно открыть командой `sudo nano ~/.bashrc` на Ubuntu и MacOS (или любом графическом текстовом редакторе). На Windows файл можно найти в корневом каталоге и открыть через текстовый редактор.

Для примера в созданном виртуальном окружении установим следующие пакеты: [black](https://github.com/psf/black) (для автоматического форматирования кода), [isort](https://github.com/timothycrosley/isort) (для сортировки импортов, т.е., имен подключаемых модулей), [mypy](http://mypy-lang.org/) (для проверки типов) и [pylint](https://www.pylint.org/) (статический анализатор кода для выявления потенциальных проблем синтаксиса):

```python
$ workon cs102
(cs102) $ pip freeze
# Установленных пакетов нет
(cs102) $ python -m pip install black isort mypy pylint
(cs102) $ pip freeze
black==24.8.0
isort==5.13.2
mypy==1.11.2
pylint==3.3.0
...
```

В этом примере мы использовали [pip](https://docs.python.org/3.5/installing/index.html) для установки новых пакетов. Иногда устанавливаемый пакет требует наличия других пакетов для своей работы, обычно эти пакеты устанавливаются автоматически (говорят «по зависимостям»). Но может возникнуть ситуация, когда вам придется вручную установить нужную библиотеку. Если вы не знаете как это сделать, то поищите ответ на stackoverflow.com, скорее всего кто-то уже столкнулся с той же проблемой, что и вы. Рекомендуется сделать это ДО попыток решить проблему через генеративную нейросеть, чтобы сократить число устраняемых в будущем проблем с зависимостями.

## Система контроля версий

Мы будем пользоваться системой контроля версий (что такое контроль версий и зачем он вам нужен можно почитать [тут](http://practical-neuroimaging.github.io/git_parable.html) и [тут](https://git-scm.com)). Все изменения, которые будут происходить с вашими работами, могут храниться локально (у вас на компьютере), а могут и удаленно, так, что вы всегда сможете продолжить работу над своим проектом. Поэтому вам необходимо зарегистрироваться на https://github.com. 

Для всех практических работ вам предоставлены шаблоны и тесты, которые расположены в [репозитории курса](https://github.com/anastasiiaCher/cs102-2026) (на скриншотах внизу везде 2024, вам нужно указать 2026). На основе репозитория курса вы можете создать свой репозиторий (через [fork](https://docs.github.com/ru/pull-requests/collaborating-with-pull-requests/working-with-forks/fork-a-repo)), для этого зарегистрируйтесь на [GitHub](https://github.com/), перейдите на страницу репозитория курса и выполните следующие шаги:

1. Найдите и нажмите в правом верхнем углу кнопку `fork` :


<img width="964" height="420" alt="image" src="https://github.com/user-attachments/assets/e557a334-082d-403e-905f-53403676a1a1" />

2. Все поля заполните, как на скриншоте. Нажмите `Create fork`:

<img width="964" height="714" alt="image" src="https://github.com/user-attachments/assets/6715fc3e-e0d8-4b79-a35c-cd0e16a31dd9" />


3. По шаблону репозитория курса был создан новый репозиторий, который теперь принадлежит вам. Его необходимо клонировать (создать копию репозитория у вас на компьютере):

```python
(cs102) ana_cher@MacBook-Pro-Anastasia ~ % git clone https://github.com/your_user_name/your_repo_name.git
Cloning into 'cs102-2026'...
Username for 'https://github.com': your_user_name
Password for 'https://you_user_name@github.com': 
remote: Enumerating objects: 15, done.
remote: Counting objects: 100% (15/15), done.
remote: Compressing objects: 100% (9/9), done.
remote: Total 15 (delta 0), reused 12 (delta 0), pack-reused 0
Receiving objects: 100% (15/15), 5.80 KiB | 2.90 MiB/s, done.
(cs102) ana_cher@MacBook-Pro-Anastasia ~ % cd cs102-2024
(cs102) ana_cher@MacBook-Pro-Anastasia cs102-2024 % ls
homework00		homework10
homework04_new		requirements.txt
```

На текущий момент вместо пароля в Github используется токен аутентификации. Создать ваш личный токен можно в настройках вашего аккаунта (https://github.com/settings/tokens). Но будьте внимательны при выборе настроек токена.

Альтернативно можно воспользоваться [SSH-ключом](https://ru.wikipedia.org/wiki/SSH), следуя [официальной инструкции](https://docs.github.com/en/authentication/connecting-to-github-with-ssh).

После выполнения команды `git clone` будет создан каталог с именем `cs102-2026`. Для быстрого перехода в этот каталог добавим alias в ваш `.bashrc` или `.zshrc`, для этого откройте терминал и выполните следующие команды:

```python
$ echo 'alias gocs102="cd /путь/к/каталогу/cs102-2026"' >> ~/.bashrc
$ . ~/.bashrc
$ gocs102
$ pwd
/путь/к/каталогу/cs102-2026
```

Теперь при вводе команды `gocs102` вы всегда и из любого расположения будете попадать в каталог `cs102-2026`, где расположен ваш репозиторий.

## Выбор редактора кода

Вам будет нужен редактор кода. Это может быть «простой» текстовый редактор с подсветкой синтаксиса и дополнительно устанавливаемыми расширениями, например, [VSCode](https://code.visualstudio.com/), [Vim](http://www.vim.org/), [Atom](https://atom.io/), [SublimeText](https://www.sublimetext.com/) или же полноценная среда разработки [PyCharm](https://www.jetbrains.com/pycharm/download/). Рекомендуется попробовать разные редакторы, потратив некоторое время на ознакомление с их возможностями и чтение документации. Использовать агентные редакторы кода вроде `Cursor` при выполннеии лабораторных работ не рекомендуется.

Найдите в вашем локальном репозитории (папке с названием cs102-2026) файл `hello_world.py` , найдите в нем ошибки синтаксиса и исправьте их. Если не получается найти шибку, можно обратиться за помощью к преподавателю практики. Оценивается на данном этапе внесение правки в файл, а не самостоятельное устранение ошибок. После внесения правок откройте терминал и выполните следующие команды:

```python
$ isort homework00/hello_world.py
$ black -l 120 homework00/hello_world.py
$ mypy homework00/hello_world.py
$ pylint homework00/hello_world.py
```

Если какая-либо из команд сообщает вам об ошибках, ваша задача – исправить эти ошибки. `pylint` будет печатать рекомендации по оптимизации вашего кода, настоятельно рекомендуется последовать этим рекомендациям и получить рейтинг кода не менее 8.00.

Когда все ошибки будут устранены выполненные вами изменения в файле `hello_world.py` необходимо зафиксировать и отправить на проверку:

```python
$ git checkout -b homework00
$ git add homework00/hello_world.py
$ git commit -m "Исправлены ошибки"
$ git push origin homework00
```

После выполнения последней команды ваши изменения окажутся на сервере. Вы увидите, что теперь можете сделать запрос на изменения (Compare & pull request), а также наличие ветки `homework00`:

<img width="964" height="136" alt="image" src="https://github.com/user-attachments/assets/f8cce722-f983-4731-b216-92d2fddb9c1f" />


Создавая пул-реквест, вы отправляете работу в основной репозиторий курса на проверку, поэтому будьте внимательны и проверяйте, в какую ветку какого репозитория вы посылаете код. Для homework00 нужно выбрать ветку homework00. В последующих работах нужно будет выбирать ветку с номером той работы, которую вы сдаете:

<img width="964" height="128" alt="image" src="https://github.com/user-attachments/assets/44912f33-892e-4ede-b6ba-26f9664d5e8e" />


Создавая пул-реквест, убедитесь, что в его названии указан номер работы, номер вашего потока и ваше ФИО, чтобы преподаватель точно знал, что вы хотите сдать на проверку:

<img width="966" height="412" alt="image" src="https://github.com/user-attachments/assets/7f5495ba-3a47-47c4-91e9-2a40b0cd8788" />


Когда пул-реквест будет отправлен, вы увидите, что в основной репозиторий послан запрос на проверку кода. Одобрение запроса происходит не всегда происходит сразу, так как делает вручную.

<img width="964" height="390" alt="image" src="https://github.com/user-attachments/assets/fd8a4f52-9c84-4b1b-8fe3-30ce0bf453f6" />


Если проверка пройдена и вы видите результат с красным крестиком, значит, какая-то проверка не прошла и надо внести правки в работу. 

<img width="964" height="156" alt="image" src="https://github.com/user-attachments/assets/8aa3286e-0900-4798-9580-9edabacd473f" />


Чтобы узнать, что пошло не так, нажмите сначала на название пул-реквеста, затем на `build`.

<img width="964" height="224" alt="image" src="https://github.com/user-attachments/assets/52fe2894-9bd9-43ff-889c-6c8d5d554c63" />


<img width="964" height="526" alt="image" src="https://github.com/user-attachments/assets/c9adfad5-45e3-4905-a6ba-cbcaa193e37a" />


В этом примере модуль `black` нашел проблему в оформлении кода. Обычно такое случается, если перед отправкой на сервер пропущен шаг `black -l 120 file_name.py`. 

Исправьте проблему и закомитьте изменения на Github. Открывать новый пул-реквест для этого не надо, достаточно внести правки в нужный файл и сделать коммит.

```python
(cs102) $ git add homework00/hello_world.py
(cs102) $ git commit -m "Описание исправлений"
(cs102) $ git push origin homework00
```

<img width="964" height="320" alt="image" src="https://github.com/user-attachments/assets/1dca5991-27f7-423a-8a7b-a965eef8abc1" />


Наличие зеленой галочки гарантирует вам 5 баллов за эту работу. За защиту работы, включающую демонстрацию выполнения всех настроек, описанных в задании, и ответы на вопросы, можно получить еще 2 балла.

Все вопросы и замечания пишите в чате в Telegram.

